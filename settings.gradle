rootProject.name = 'dsml'

// declare subprojects
include 'parser'
include 'engine'

// define subproject directories
rootProject.children.each { project ->
    project.projectDir = file("spec/$project.name")
}
